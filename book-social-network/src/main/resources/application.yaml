spring:
  application:
    name: book-social-network
  profiles:
    active: mysql
  servlet:
    multipart:
      max-file-size: 50MB
springdoc:
  default-produces-media-type: application/json
server:
  port: 8080
  servlet:
    context-path: /api/v1/

